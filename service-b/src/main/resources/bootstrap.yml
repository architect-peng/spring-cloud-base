
spring:
  application:
    name: service-b
  cloud:
    config:
      uri: http://localhost:9090/
      name: native
      profile: common
eureka:
  instance:
    appname: service-b
    hostname: 127.0.0.1
    lease-renewal-interval-in-seconds: 5
server:
  port: 8081